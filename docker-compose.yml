version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    command: redis-server --appendonly yes

  app:
    build: .
    ports: ["8083:8083"]
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on: [redis]
    restart: unless-stopped
